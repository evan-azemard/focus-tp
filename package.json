{
  "name": "focus",
  "version": "1.0.0",
  "scripts": {
    "dev:client": "pnpm --filter focus-client dev",
    "dev:server": "pnpm --filter server dev",
    "dev": "concurrently \"pnpm dev:client\" \"pnpm dev:server\""
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}